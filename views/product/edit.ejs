<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Edit </title>
</head>
<body>
  <h1>Edit product  <%= products.name %></h1>
  <form action="/product/<%=products._id%>/?_method=PUT" method="POST" enctype="multipart/form-data">
        <label for="name">Name of Dish :</label>
        <input type="text" name="name" id="name" value="<%= products.name %>"/>
        <label for="description">Description of Dish :</label>
        <input type="text" name="description" id="description" value="<%= products ? products.description : '' %>">
        <label for="image">Dish Image:</label>
        <input type="file" name="image" id="image">
        <button type="submit">Update Dish</button>
  </form>
  <h3>Image:</h3>
    <% if (products.image) { %>
      <img src="/uploads/<%= products.image %>" alt="<%= products.name %>" class="product-pic1">
    <% } %>
  <a href="/product">Back to products list</a>
</body>
</html>
